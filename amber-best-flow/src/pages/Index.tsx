/**
 * Index Page Component
 * This file is kept for backward compatibility but is no longer used for routing.
 * All routing is now handled in App.tsx with React Router.
 * This file can be removed in the future if not needed.
 */

import { AuthProvider } from "@/contexts/AuthContext";

// This component is no longer used - routing is handled in App.tsx
// Keeping it for backward compatibility
const Index = () => {
  return null;
};

export default Index;
